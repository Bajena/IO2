using System;
using Common.Problems;
using Microsoft.VisualStudio.TestTools.UnitTesting;

namespace Tests
{
    [TestClass]
    public class ProblemsTests
    {
        //[TestMethod]
        //public void MultiplyProblemSerialize()
        //{
        //    var problem = new MultiplyProblemData(4, 3);
        //    var serialized = problem.Serialize();
        //    var deserialized = MultiplyProblemData.Deserialize(serialized);

        //    Assert.AreEqual(problem.Multiplier, deserialized.Multiplier);
        //    Assert.AreEqual(problem.Number, deserialized.Number);
        //}
    }
}
